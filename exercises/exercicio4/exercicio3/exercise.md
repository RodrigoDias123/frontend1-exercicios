Criar um pequeno sistema com:

Campo para escrever a password 

Botão “Guardar” que adiciona a password a um array de objetos e guarda no localStorage.

Guardar no localStorage com JSON.stringify() / JSON.parse() para persistência, e mostrar as passwords guardadas abaixo.

